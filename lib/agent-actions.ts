// Agent Actions - определение намерений пользователя

export type AgentAction = 
  | 'analyze'      // Глубокий анализ документа
  | 'compare'      // Сравнение документов
  | 'summarize'    // Краткое содержание
  | 'extract'      // Извлечение фактов
  | 'qa'           // Простой вопрос-ответ (по умолчанию)

export interface AgentIntent {
  action: AgentAction
  target?: 'latest' | 'all' | 'specific'
  documentId?: string
  confidence: number
}

export interface SourceCitation {
  documentId: string
  documentTitle: string
  chunkId: string
  quote: string
  similarity: number
}

export interface AgentResponse {
  reply: string
  sources?: SourceCitation[]
  intent: AgentIntent
  conversationId?: string
}

/**
 * Определяет намерение пользователя из запроса
 */
export function detectIntent(query: string): AgentIntent {
  const lowerQuery = query.toLowerCase()
  
  // Анализ
  if (
    lowerQuery.includes('анализ') ||
    lowerQuery.includes('разбери') ||
    lowerQuery.includes('проанализируй')
  ) {
    const isLatest = 
      lowerQuery.includes('последн') || 
      lowerQuery.includes('новый') ||
      lowerQuery.includes('свежий')
    
    const isAll = 
      lowerQuery.includes('все') ||
      lowerQuery.includes('всех') ||
      lowerQuery.includes('каждую')
    
    return {
      action: 'analyze',
      target: isLatest ? 'latest' : isAll ? 'all' : 'latest',
      confidence: 0.9
    }
  }
  
  // Сравнение
  if (
    lowerQuery.includes('сравни') ||
    lowerQuery.includes('отличи') ||
    lowerQuery.includes('похож')
  ) {
    return {
      action: 'compare',
      target: 'all',
      confidence: 0.85
    }
  }
  
  // Суммаризация
  if (
    lowerQuery.includes('кратко') ||
    lowerQuery.includes('резюм') ||
    lowerQuery.includes('суть') ||
    lowerQuery.includes('главное') ||
    (lowerQuery.includes('о чем') || lowerQuery.includes('о ком')) // "о чем последняя колонка?"
  ) {
    const isLatest = 
      lowerQuery.includes('последн') || 
      lowerQuery.includes('новый') ||
      lowerQuery.includes('свежий')
    return {
      action: 'summarize',
      target: isLatest ? 'latest' : 'all',
      confidence: 0.85
    }
  }
  
  // Извлечение данных
  if (
    lowerQuery.includes('когда') ||
    lowerQuery.includes('сколько') ||
    lowerQuery.includes('кто') ||
    lowerQuery.includes('где') ||
    lowerQuery.includes('список')
  ) {
    return {
      action: 'extract',
      confidence: 0.7
    }
  }
  
  // По умолчанию - обычный Q&A
  return {
    action: 'qa',
    confidence: 0.6
  }
}

/**
 * Системные промпты для разных типов действий
 */
export const AGENT_PROMPTS = {
  analyze: `Ты — аналитик-эксперт. Твоя задача — сделать глубокий структурированный анализ предоставленного текста.

Включи в анализ:
1. **Главная тема**: О чем текст в одном предложении
2. **Ключевые идеи**: 3-5 основных тезисов с пояснениями
3. **Аргументация**: Какие доказательства/примеры приводятся
4. **Выводы**: Какие выводы делает автор
5. **Критическая оценка**: Сильные/слабые стороны аргументации

Используй структуру с заголовками и подпунктами. Будь конкретен и ссылайся на факты из текста.`,

  compare: `Ты — компаративный аналитик. Твоя задача — сравнить несколько текстов и выявить общее и различия.

Структура сравнения:
1. **Общие темы**: Что объединяет тексты
2. **Ключевые различия**: Где позиции/факты расходятся
3. **Уникальные элементы**: Что есть только в одном тексте
4. **Синтез**: Общая картина из всех источников

Будь объективен, не добавляй информацию которой нет в текстах.`,

  multi_analyze: `Ты — стратегический аналитик больших данных. Твоя задача — проанализировать множество документов и выявить общие паттерны, тренды и инсайты.

Структура мультидокументного анализа:
1. **Обзор**: Сколько документов, какой период, общая тема
2. **Ключевые темы**: Топ-5 тем которые повторяются
3. **Эволюция идей**: Как менялись взгляды/факты со временем
4. **Противоречия**: Где документы расходятся во мнениях
5. **Уникальные находки**: Что упоминается только в одном источнике
6. **Выводы**: Общая картина и главные инсайты

Используй конкретные примеры из текстов. Группируй похожие идеи.`,

  summarize: `Ты — эксперт по суммаризации текстов. Твоя задача — создать краткое, но информативное резюме, сохранив все ключевые идеи.

Структура резюме:
**📌 Суть в одном предложении**
[Главная идея текста - 1 предложение]

**🔑 Ключевые моменты:**
• [Первая важная идея]
• [Вторая важная идея]
• [Третья важная идея]
• [Четвертая важная идея, если есть]

**💡 Основной вывод:**
[Главный месседж автора - 1-2 предложения]

Правила:
- Сохраняй конкретные факты, имена, даты, цифры
- Не добавляй свои интерпретации - только что есть в тексте
- Объем: 100-200 слов
- Используй эмодзи для структуры (как в примере)`,

  extract: `Ты — эксперт по извлечению данных. Твоя задача — найти и структурировано представить конкретную информацию.

Формат ответа:
- Прямой ответ на вопрос (факт, число, дата, имя)
- Контекст: откуда эта информация (цитата из текста)
- Дополнительно: связанные факты если релевантны

Если информации нет — скажи честно. Не выдумывай.`,

  qa: `Ты — экспертный ассистент по работе с документами. Твоя задача — давать точные, информативные ответы на основе предоставленного контекста.

Правила ответа:
1. **Используй только информацию из контекста** — не выдумывай факты
2. **Будь конкретен** — называй имена, даты, цифры из текста
3. **Структурируй ответ** — если вопрос сложный, разбей на пункты
4. **Цитируй важное** — используй кавычки для ключевых фраз из текста
5. **Признавай ограничения** — если информации недостаточно, скажи об этом

Формат:
- Прямой ответ на вопрос (2-4 предложения)
- Важные детали или контекст при необходимости
- Если нет информации: "В предоставленных документах нет информации по этому вопросу"

Отвечай на русском языке, профессионально и по существу.`
}

/**
 * Форматирует ответ с цитатами источников
 */
export function formatResponseWithSources(
  reply: string, 
  sources: SourceCitation[]
): string {
  if (!sources || sources.length === 0) return reply
  
  let formatted = reply + '\n\n---\n\n**📚 Источники:**\n\n'
  
  sources.forEach((source, i) => {
    formatted += `${i + 1}. **${source.documentTitle}**\n`
    formatted += `   💬 _"${source.quote.substring(0, 150)}${source.quote.length > 150 ? '...' : ''}"_\n`
    formatted += `   🎯 Релевантность: ${(source.similarity * 100).toFixed(0)}%\n\n`
  })
  
  return formatted
}

/**
 * Извлекает релевантные цитаты из matches
 */
export function extractCitations(
  matches: any[],
  documentMap: Map<string, string>
): SourceCitation[] {
  return matches.slice(0, 3).map(match => ({
    documentId: match.document_id || match.id,
    documentTitle: documentMap.get(match.document_id || match.id) || 'Неизвестный документ',
    chunkId: match.id,
    quote: match.content?.substring(0, 300) || '',
    similarity: match.similarity || 0
  }))
}
