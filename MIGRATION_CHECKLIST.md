# ✅ Чек-лист миграции на Vercel AI SDK

## Что уже сделано:

- [x] Установлены пакеты: `ai` и `@ai-sdk/openai`
- [x] Создан новый файл `lib/embedding-ai.ts` с Vercel AI SDK
- [x] Обновлен `/api/ingest/route.ts` для использования нового embedding
- [x] Обновлен `/api/chat/route.ts` для использования нового embedding
- [x] Создан SQL файл миграции `supabase/migration-vercel-ai.sql`

## Что нужно сделать:

### 1. Применить SQL миграцию ⚠️ ВАЖНО!

Откройте: https://supabase.com/dashboard/project/hukfgitwamcwsiyxlhyb/editor

Скопируйте и выполните содержимое файла: `supabase/migration-vercel-ai.sql`

⚠️ ВНИМАНИЕ: Это удалит все существующие документы!

### 2. Проверить настройку

После применения миграции запустите тест:

```bash
node test-vercel-ai-setup.js
```

Все 4 теста должны пройти успешно.

### 3. Запустить dev сервер

```bash
npm run dev
```

### 4. Протестировать загрузку файла

```bash
curl -X POST http://localhost:3000/api/ingest \
  -F "file=@test-upload.txt" \
  -F "sourceId=c5aab739-7112-4360-be9e-45edf4287c42"
```

Ожидаемый результат:
```json
{
  "success": true,
  "document_id": "...",
  "totalChunks": 5,
  "embeddingModel": "text-embedding-3-small",
  "embeddingDimension": 1536
}
```

### 5. Протестировать поиск

```bash
curl -X POST http://localhost:3000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"query": "что такое векторный поиск?"}'
```

## Изменения в архитектуре:

### До миграции:
- `documents`: vector(384) - Hugging Face
- `document_chunks`: vector(768) - Ollama/Fireworks
- Разные embedding провайдеры в разных API

### После миграции:
- `documents`: только метаданные (title, description, source_url)
- `document_chunks`: vector(1536) - OpenAI via Vercel AI SDK
- Единый embedding провайдер для всех API

### Новая структура:
```
documents (метаданные документов)
  ├─ id, title, description, source_url, source_id, created_at
  └─ document_chunks (чанки с embeddings)
      ├─ id, document_id, chunk_index, content
      ├─ embedding: vector(1536)
      ├─ checksum, metadata, created_at
      └─ функция: match_documents(query_embedding, match_count)
```

## Преимущества новой системы:

✅ **Единый провайдер**: OpenAI через Vercel AI SDK  
✅ **Лучшее качество**: text-embedding-3-small (SOTA)  
✅ **Простота**: один embedding provider для всего  
✅ **Стандарт**: следует best practices Vercel  
✅ **Масштабируемость**: легко добавить streaming, RAG patterns

## Если что-то пошло не так:

1. **Ошибка при миграции SQL**: 
   - Проверьте что расширение `vector` установлено
   - Убедитесь что нет активных соединений к таблицам

2. **Ошибка embedding generation**:
   - Проверьте OPENAI_API_KEY в .env.local
   - Убедитесь что ключ валидный и имеет кредиты

3. **Ошибка при поиске**:
   - Проверьте что функция match_documents создана
   - Убедитесь что есть хотя бы один документ в БД

## Стоимость использования:

OpenAI text-embedding-3-small:
- $0.02 per 1M tokens
- ~750 слов на 1000 токенов
- Пример: 1000 документов по 500 слов = ~$0.01

Очень дешево для продакшена! 🎉
